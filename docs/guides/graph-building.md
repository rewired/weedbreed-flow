# Graph Building Guide

1. Open the planner canvas from the zone view.
2. Drag cards onto the canvas; each node exposes resource-specific handles (`energy:in`).
3. Connect matching handles; mixed resource types are rejected with banner message.
4. Double-click an edge capacity badge to edit throughput.
5. Run the simulation to compare KPI hashes against golden fixtures.