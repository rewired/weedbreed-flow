# Implementation Plan: [FEATURE]

**Branch**: [###-feature-name] | **Date**: [DATE] | **Spec**: [link]
**Input**: Feature specification from `/specs/[###-feature-name]/spec.md`

**Note**: Generated by `/speckit.plan`. Refer to `.specify/memory/constitution.md` before editing.

## Summary

[Describe the primary requirement and the planned approach grounded in research findings.]

## Technical Context

**Language/Version**: Node.js 22 LTS (baseline) or NEEDS CLARIFICATION  
**Package Manager**: pnpm (baseline) or NEEDS CLARIFICATION  
**UI Stack**: React, React Flow, Tailwind CSS, Vite (baseline) or NEEDS CLARIFICATION  
**Documentation Standard**: JSDoc plus markdown guides (baseline) or NEEDS CLARIFICATION  
**Storage**: [e.g., local JSON persistence, IndexedDB, none] or NEEDS CLARIFICATION  
**Testing Strategy**: [e.g., Vitest, Playwright, custom harness] or NEEDS CLARIFICATION  
**Target Platform**: Desktop web (baseline) or NEEDS CLARIFICATION  
**Performance Goals**: [State tick budget, latency, frame rate targets] or NEEDS CLARIFICATION  
**Determinism Guarantees**: [Document seeded RNG, tick ordering, fixture coverage] or NEEDS CLARIFICATION  
**Constraints**: [e.g., offline-only, memory bounds, accessibility commitments] or NEEDS CLARIFICATION  
**Scale/Scope**: [Captured graph sizes, zones, plugin counts] or NEEDS CLARIFICATION

## Constitution Check

*Gate: list the WeedBreed Flow principles that apply, the rationale, and PASS/FAIL status. Work MAY NOT proceed while any FAIL remains.*

## Project Structure

### Documentation (feature scope)

```
specs/[###-feature-name]/
  plan.md         # /speckit.plan output (this file)
  research.md     # Phase 0 deliverable (resolved unknowns)
  data-model.md   # Phase 1 deliverable (entities and states)
  quickstart.md   # Phase 1 deliverable (setup instructions)
  contracts/      # Phase 1 deliverable (API or schema contracts)
  tasks.md        # Phase 2 deliverable (/speckit.tasks, not created here)
```

### Source Code (repository root)

```
# Replace with the actual layout once decided. Remove unused options.

src/
  app/
  components/
  domains/
  sim/
  styles/

public/

packages/        # optional, list if plan introduces packages

scripts/

tests/
  unit/
  integration/
  contract/
```

**Structure Decision**: [Document the chosen layout and reference existing or planned directories.]

## Complexity Tracking

*Use only when a constitution gate is intentionally violated and requires mitigation planning.*

| Violation | Why Needed | Mitigation Timeline |
|-----------|------------|---------------------|
| [Principle ID] | [Reason the deviation is unavoidable] | [Plan to return to compliance] |