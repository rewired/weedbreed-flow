{
  "name": "@weedbreed/sim-fixtures",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest --run",
    "generate": "node dist/run.js"
  },
  "dependencies": {
    "@weedbreed/sim-core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^1.5.0"
  }
}